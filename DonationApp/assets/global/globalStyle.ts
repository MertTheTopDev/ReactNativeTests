import { StyleSheet } from "react-native";
import GlobalFunc from "./GlobalFunc";

const globalStyle = StyleSheet.create({
  background: {
    backgroundColor: GlobalFunc.color.white
  },
  flex: {
    flex: 1
  },
  marginBottom24: {
    marginBottom: 24
  }
});

export default globalStyle;